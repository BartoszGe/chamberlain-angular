export class Product {
  constructor(
    public id: number,
    public name: string,
    public type: string,
    public amount: 0,
    public price: number,
    public measureType: string,
    public description: string
  ) {
  }
}
